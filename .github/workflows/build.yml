name: CI

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Validate YAML files
        uses: ibiqlik/action-yamllint@v1
        with:
          configFile: .github/.yamllint.yml